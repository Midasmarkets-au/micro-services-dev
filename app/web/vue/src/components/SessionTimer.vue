<template>
  <!-- SessionTimer disabled: session expiry managed by auth service (cookie Max-Age / JWT exp) -->
</template>

<script setup lang="ts">
// Session timer logic disabled — token lifecycle is controlled entirely by the
// auth service. Logout is only triggered by explicit user action.
</script>
