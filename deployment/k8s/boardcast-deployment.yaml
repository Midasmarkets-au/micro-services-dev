# ============================================================================
#  boardcast — Rust SSE 消息推送 + gRPC 广播服务
#  端口：8081 (HTTP/SSE)、50052 (gRPC)
# ============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: boardcast
  labels:
    app: boardcast
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boardcast
  template:
    metadata:
      labels:
        app: boardcast
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      imagePullSecrets:
        - name: ecr-registry
      containers:
        - name: boardcast
          image: 961341529237.dkr.ecr.ap-southeast-2.amazonaws.com/boardcast:latest
          imagePullPolicy: Always
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - name: http
              containerPort: 8081
            - name: grpc
              containerPort: 50052
          env:
            - name: HTTP_ADDR
              value: "[::]:8081"
            - name: GRPC_ADDR
              value: "[::]:50052"
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
