apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: production
spec:
  limits:
    cpu: "8"      # max ~4 nodes x c6g.large (2 vCPU each)
    memory: 16Gi  # max ~4 nodes x c6g.large (4 GiB each)
  template:
    metadata:
      labels:
        workload-type: production
    spec:
      nodeClassRef:
        group: eks.amazonaws.com
        kind: NodeClass
        name: default
      requirements:
        - key: kubernetes.io/arch
          operator: In
          values: ["arm64"]
        - key: karpenter.sh/capacity-type
          operator: In
          values: ["on-demand"]
        - key: node.kubernetes.io/instance-type
          operator: In
          values: ["c6g.large", "c6gd.large", "m6g.large"]
      taints:
        - key: workload-type
          value: production
          effect: NoSchedule
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 30s
    budgets:
      - nodes: "1"
  weight: 20
